# Example environment variables (safe to commit)
# Copy to .env and replace with your real secrets

EMAIL_USER=example@gmail.com
EMAIL_PASS=example-app-password

# Optional
# PORT=5000
# NODE_ENV=development
